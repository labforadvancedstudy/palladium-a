// Test for the bootstrap parser
// Demonstrates parsing capabilities

fn test_parser_validation() {
    print("=== Bootstrap Parser Test ===\n");
    
    // Test valid let statements
    let x = 42;
    let name = "Palladium";
    let flag = true;
    
    // Test expressions
    print("Testing expressions:");
    print_int(x);
    print(name);
    
    // Test arithmetic
    let a = 10;
    let b = 20;
    let c = a + b;
    
    print("Arithmetic result:");
    print_int(c);
    
    // Test string operations
    let greeting = "Hello, ";
    let target = "Bootstrap!";
    let message = string_concat(greeting, target);
    print(message);
    
    print("\n✓ Parser test complete!");
}

fn main() {
    test_parser_validation();
    
    print("\nThis file can be used to test the parser!");
    print("Run: ./parser_minimal < bootstrap_parser_test.pd");
}