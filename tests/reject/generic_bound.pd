// NEGATIVE TEST -- a trait bound on a generic parameter does not parse.
//
// `fn f<T: Clone>(x: T)` is the ordinary spelling of a bounded generic. The
// parameter list parser accepts only bare names (and an all-uppercase name is
// reclassified as a const argument, src/parser/mod.rs:2268-2278), so the `:`
// that introduces the bound terminates parsing.
fn f<T: Clone>(x: T) {}

fn main() {}
