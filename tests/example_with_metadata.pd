//@ description: Example test with metadata annotations
//@ tags: example, metadata, demo
//@ expect-exit-code: 0
//@ compile-only: false

// This test demonstrates the test runner metadata system

fn main() {
    print("Hello from test with metadata!");
    print("This test should pass successfully.");
    
    // Test some basic operations
    let x = 42;
    let y = 13;
    let sum = x + y;
    
    print("The sum of " + int_to_string(x) + " and " + int_to_string(y) + " is " + int_to_string(sum));
    
    // Test should exit cleanly
    print("Test completed successfully!");
}