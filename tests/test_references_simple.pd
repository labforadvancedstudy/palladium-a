// Simple test for reference types

fn main() {
    // Test immutable reference
    let x: i32 = 42;
    let ref_x: &i32 = &x;
    
    // Test mutable reference
    let mut y: i32 = 10;
    let ref_y: &mut i32 = &mut y;
    *ref_y = 20;
    
    print_int(42); // Just print something
}