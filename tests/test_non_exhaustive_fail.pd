// This file should fail compilation due to non-exhaustive match

enum Status {
    Success,
    Error,
    Pending,
}

fn main() {
    let status = Status::Success;
    
    // This match is non-exhaustive - missing Pending
    match status {
        Status::Success => print("Success!"),
        Status::Error => print("Error!"),
    }
}