// Test missing variant in exhaustiveness checking

enum TrafficLight {
    Red,
    Yellow,
    Green,
}

fn main() {
    let light = TrafficLight::Red;
    
    // This should fail - missing Yellow
    match light {
        TrafficLight::Red => print("Stop!"),
        TrafficLight::Green => print("Go!"),
    }
}