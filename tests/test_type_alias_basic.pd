// Test basic type aliases
type NodeId = i32;
type MyString = String;

fn main() {
    let id: NodeId = 42;
    let name: MyString = "Hello";
    
    print_int(id);
    print_string(name);
}

fn print_int(x: i32) {
    // Print the value
}

fn print_string(s: String) {
    // Print the string
}