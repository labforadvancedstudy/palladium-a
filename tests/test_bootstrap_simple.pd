// Simple tests for Palladium bootstrap components
// Tests core functionality without nested functions

// Test string utilities
fn test_string_operations() {
    print("=== Testing String Operations ===");
    
    // Test string_len
    let s1 = "Hello";
    let len1 = string_len(s1);
    if len1 == 5 {
        print("✓ string_len works correctly");
    } else {
        print("✗ string_len failed");
    }
    
    // Test string_concat
    let s2 = " World";
    let s3 = string_concat(s1, s2);
    if string_eq(s3, "Hello World") {
        print("✓ string_concat works correctly");
    } else {
        print("✗ string_concat failed");
    }
    
    // Test string_substring
    let sub = string_substring("Palladium", 0, 4);
    if string_eq(sub, "Pall") {
        print("✓ string_substring works correctly");
    } else {
        print("✗ string_substring failed");
    }
    
    // Test string_char_at
    let ch = string_char_at("ABC", 1);
    if ch == 66 {  // ASCII 'B'
        print("✓ string_char_at works correctly");
    } else {
        print("✗ string_char_at failed");
    }
}

// Test array operations
fn test_array_operations() {
    print("\n=== Testing Array Operations ===");
    
    // Test array initialization
    let arr = [10, 20, 30, 40, 50];
    
    // Test array indexing
    if arr[0] == 10 {
        print("✓ Array index 0 correct");
    } else {
        print("✗ Array index 0 failed");
    }
    
    if arr[4] == 50 {
        print("✓ Array index 4 correct");
    } else {
        print("✗ Array index 4 failed");
    }
    
    // Test array with repeat syntax
    let zeros = [0; 3];
    if zeros[0] == 0 {
        if zeros[1] == 0 {
            if zeros[2] == 0 {
                print("✓ Array repeat syntax works");
            } else {
                print("✗ Array repeat failed at index 2");
            }
        } else {
            print("✗ Array repeat failed at index 1");
        }
    } else {
        print("✗ Array repeat failed at index 0");
    }
}

// Test control flow
fn test_control_flow() {
    print("\n=== Testing Control Flow ===");
    
    // Test for loop
    let mut sum = 0;
    for i in 0..5 {
        sum = sum + i;
    }
    if sum == 10 {  // 0+1+2+3+4 = 10
        print("✓ For loop works correctly");
    } else {
        print("✗ For loop failed");
    }
    
    // Test for loop with break
    let mut count = 0;
    for i in 0..10 {
        if i == 5 {
            break;
        }
        count = count + 1;
    }
    if count == 5 {
        print("✓ For loop with break works");
    } else {
        print("✗ For loop with break failed");
    }
    
    // Test while loop
    let mut i = 0;
    let mut sum2 = 0;
    while i < 5 {
        sum2 = sum2 + i;
        i = i + 1;
    }
    if sum2 == 10 {
        print("✓ While loop works correctly");
    } else {
        print("✗ While loop failed");
    }
}

// Test enum and pattern matching
fn test_enums() {
    print("\n=== Testing Enums ===");
    
    enum Status {
        Success,
        Error(String),
    }
    
    let s1 = Status::Success;
    let s2 = Status::Error("Failed");
    
    // Test pattern matching
    match s1 {
        Status::Success => print("✓ Pattern match Success works"),
        Status::Error(_) => print("✗ Pattern match failed"),
    }
    
    match s2 {
        Status::Success => print("✗ Pattern match failed"),
        Status::Error(_) => print("✓ Pattern match Error works"),
    }
}

// Test struct operations
fn test_structs() {
    print("\n=== Testing Structs ===");
    
    struct Point {
        x: i64,
        y: i64,
    }
    
    let p = Point { x: 10, y: 20 };
    
    if p.x == 10 {
        print("✓ Struct field x access works");
    } else {
        print("✗ Struct field x access failed");
    }
    
    if p.y == 20 {
        print("✓ Struct field y access works");
    } else {
        print("✗ Struct field y access failed");
    }
}

// Test boolean operations
fn test_boolean_ops() {
    print("\n=== Testing Boolean Operations ===");
    
    let t = true;
    let f = false;
    
    if t {
        print("✓ Boolean true works");
    } else {
        print("✗ Boolean true failed");
    }
    
    if f == false {
        print("✓ Boolean false works");
    } else {
        print("✗ Boolean false failed");
    }
    
    if t != f {
        print("✓ Boolean inequality works");
    } else {
        print("✗ Boolean inequality failed");
    }
}

// Test arithmetic operations
fn test_arithmetic() {
    print("\n=== Testing Arithmetic ===");
    
    let a = 10;
    let b = 3;
    
    if a + b == 13 {
        print("✓ Addition works");
    } else {
        print("✗ Addition failed");
    }
    
    if a - b == 7 {
        print("✓ Subtraction works");
    } else {
        print("✗ Subtraction failed");
    }
    
    if a * b == 30 {
        print("✓ Multiplication works");
    } else {
        print("✗ Multiplication failed");
    }
    
    if a / b == 3 {
        print("✓ Division works");
    } else {
        print("✗ Division failed");
    }
    
    if a % b == 1 {
        print("✓ Modulo works");
    } else {
        print("✗ Modulo failed");
    }
}

fn main() {
    print("=== Palladium Bootstrap Component Tests ===\n");
    
    test_string_operations();
    test_array_operations();
    test_control_flow();
    test_enums();
    test_structs();
    test_boolean_ops();
    test_arithmetic();
    
    print("\n=== All Tests Complete ===");
    print("Bootstrap components verified!");
    print("Ready for self-hosting! 🚀");
}