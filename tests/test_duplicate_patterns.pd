// Test duplicate pattern detection

enum Status {
    Ok,
    Error,
    Pending,
}

fn main() {
    let status = Status::Ok;
    
    // Duplicate Ok pattern
    match status {
        Status::Ok => print("Success"),
        Status::Error => print("Failed"),
        Status::Ok => print("Success again"), // Duplicate!
        Status::Pending => print("Waiting"),
    }
}