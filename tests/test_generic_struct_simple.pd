// Test that generic struct definitions are parsed correctly

// Generic Box type
struct Box<T> {
    value: T,
}

// Generic Pair type  
struct Pair<T, U> {
    first: T,
    second: U,
}

// Non-generic struct for comparison
struct Point {
    x: i32,
    y: i32,
}

fn main() {
    // For now, just test non-generic structs
    let p = Point { x: 10, y: 20 };
    print_int(p.x);
    print_int(p.y);
    
    // TODO: Once generic struct instantiation is implemented,
    // we can test generic structs here
    print("Generic structs defined successfully!");
}