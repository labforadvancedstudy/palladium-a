// Fibonacci sequence calculator (unrolled due to no loops yet)
// Calculates the first 15 Fibonacci numbers

fn main() -> i32 {
    print("=== Fibonacci Sequence Calculator ===");
    print("First 15 Fibonacci numbers:");
    print("");
    
    // Initialize first two numbers
    let fib0 = 0;
    let fib1 = 1;
    
    print("F(0) = ");
    print_int(fib0);
    print("F(1) = ");
    print_int(fib1);
    
    // Calculate manually (no loops available)
    let fib2 = fib0 + fib1;
    print("F(2) = ");
    print_int(fib2);
    
    let fib3 = fib1 + fib2;
    print("F(3) = ");
    print_int(fib3);
    
    let fib4 = fib2 + fib3;
    print("F(4) = ");
    print_int(fib4);
    
    let fib5 = fib3 + fib4;
    print("F(5) = ");
    print_int(fib5);
    
    let fib6 = fib4 + fib5;
    print("F(6) = ");
    print_int(fib6);
    
    let fib7 = fib5 + fib6;
    print("F(7) = ");
    print_int(fib7);
    
    let fib8 = fib6 + fib7;
    print("F(8) = ");
    print_int(fib8);
    
    let fib9 = fib7 + fib8;
    print("F(9) = ");
    print_int(fib9);
    
    let fib10 = fib8 + fib9;
    print("F(10) = ");
    print_int(fib10);
    
    let fib11 = fib9 + fib10;
    print("F(11) = ");
    print_int(fib11);
    
    let fib12 = fib10 + fib11;
    print("F(12) = ");
    print_int(fib12);
    
    let fib13 = fib11 + fib12;
    print("F(13) = ");
    print_int(fib13);
    
    let fib14 = fib12 + fib13;
    print("F(14) = ");
    print_int(fib14);
    
    // Check if the 10th number is correct (should be 55)
    print("");
    print("=== Verification ===");
    if fib10 == 55 {
        print("[OK] F(10) = 55 is correct!");
    } else {
        print("[ERROR] Error in calculation!");
    }
    
    // Golden ratio approximation check
    // fib(n+1) / fib(n) approaches golden ratio (1.618...)
    // We'll check if fib14/fib13 is close to 1618/1000
    let ratio_times_1000 = (fib14 * 1000) / fib13;
    print("");
    print("Golden ratio approximation:");
    print("F(14)/F(13) * 1000 = ");
    print_int(ratio_times_1000);
    print("(Should be close to 1618)");
    
    // Sum of first 10 fibonacci numbers
    let sum_first_10 = fib0 + fib1 + fib2 + fib3 + fib4 + fib5 + fib6 + fib7 + fib8 + fib9;
    print("");
    print("Sum of first 10 Fibonacci numbers: ");
    print_int(sum_first_10);
    
    return 0;
}