// Palladium Standard Library
// "The foundation upon which legends are built"

// Core modules
pub mod option;
pub mod result;
pub mod collections;
pub mod string;
pub mod io;
pub mod math;
pub mod mem;
pub mod traits;
pub mod time;
pub mod process;
pub mod sync;
pub mod fs;
pub mod net;
pub mod thread;
pub mod env;
pub mod async;

// Re-export commonly used items at std level
pub use option::{Option, some, none};
pub use result::{Result, Error, ok, err};
pub use collections::{Vec, HashMap};
pub use traits::*;

// Version information
pub const VERSION: &str = "0.1.0";
pub const VERSION_MAJOR: u32 = 0;
pub const VERSION_MINOR: u32 = 1;
pub const VERSION_PATCH: u32 = 0;

// Standard library initialization
pub fn init() {
    // Initialize subsystems if needed
    // Currently empty but reserved for future use
}