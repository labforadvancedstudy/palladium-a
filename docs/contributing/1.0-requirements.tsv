# Palladium 1.0 requirement manifest — CLOSED INVENTORY.
#
# WHAT THIS IS, AND WHAT IT IS NOT. One row per thing the normative specification
# (language-spec.md Part I, N1-N14) requires, plus the one feature-list item kept
# in 1.0 (C FFI). It is the ENUMERATION. It is deliberately NOT the gate.
#
# 1.0 IS `make thesis-exit`: bootstrap/pdc.pd rewritten in the differentiated
# dialect — ref/ref mut with inferred regions, a discharged #[total], inferred
# effects and no async/await — still reaching a byte-identical stage1/stage2
# fixed point, with a second witness program meeting the same conditions.
#
# WHAT `satisfied` MEANS, AND THE QUESTION THAT CATCHES A ROW WHOSE TRUTH IS
# CONDITIONAL. `satisfied` means WITNESSED BY THE CITED EVIDENCE — an instance —
# never "holds for every program". The useful test on any row is not "is this true
# today" but: WHAT WOULD HAVE TO BE TRUE OF THE PROGRAM UNDER TEST, AND DOES THIS
# ROW SAY IT? A row that survives only because no witness exercises an excluded
# feature has the same defect as one that is simply wrong, and it is harder to see.
#
# The instance that produced this paragraph: SH-01's byte-identical fixed point is
# green because PBS-1 excludes BOTH imports and generics, and there are two
# independent unordered emission sources — one per exclusion, separated by
# measurement, not inference. The row asserted the property and stated neither
# condition. It states both now, and SH-05 carries both obligations.
#
# WHY NOT "the inventory has no unmet rows". Because that is a COMPLETENESS
# criterion, and completeness criteria are what this repository spent M1 burning
# out: `progress: 85%`, "Bootstrap 100% Complete", "Self-Hosting 100%". Draw the
# line on an inventory and the same disease returns under a new name. A vacuous
# fixture can print "unimplemented" and PASS; a compiler cannot compile itself
# vacuously. The fixed point is the one artifact here that structurally cannot
# lie, so it is what the definition rests on, and scope follows from what the
# compiler actually uses rather than from argument.
#
# WHY THE ENUMERATION STILL EXISTS. So that nothing is dropped silently. A
# requirement the thesis does not exercise is marked `post-1.0` HERE, in writing,
# with an owner — it is not deleted, and it is not quietly absent. The previous
# draft of this file made the enumeration the gate; that criterion went green
# with all three differentiators unimplemented, because N7/N8/N9 anchor no rows
# in feature-index.toml precisely BECAUSE they are unimplemented.
#
# CLOSED, in the same sense as tests/conformance-manifest.txt:
#   - Every row needs an owner, an evidence kind, an evidence locator and a
#     disposition.
#   - A `satisfied` row whose evidence stops resolving turns the gate RED. That
#     is what stops the debt inventories from shrinking by deletion: an
#     `#[ignore]` test can be deleted and cargo's list simply gets shorter, but
#     a row here that names it fails.
#   - Deleting a row, moving it between milestones, or moving it to `post-1.0`
#     is a CONTRACT TRANSITION and is reviewed as one.
#   - RECONCILIATION, both directions. Every red row in either debt inventory
#     must map to a row here, and every `owed` row here must name evidence that
#     does not yet pass:
#       * conformance: every `xfail` and `vacuous` row in
#         tests/conformance-manifest.txt must appear in the evidence column of at
#         least one row here. That is checkable today, by path.
#       * Rust tests: every #[ignore = "XFAIL: ..."] must carry a requirement id
#         — `req: N5-06` — and that id must exist here. THIS DOES NOT HOLD YET:
#         the reasons carry a milestone, not a requirement, so today's
#         reconciliation of that inventory is by review, not by command. Adding
#         the id is an edit to tests/ and lands with F9's re-tagging.
#
# COLUMNS (TAB-separated, all NINE mandatory, `-` for N/A):
#   id  milestone  source  requirement  evidence-kind  evidence  status  disposition
#   fingerprint
#
# evidence-kind:
#   fixture     a conformance row of class `run` with a recorded transcript.
#               POSITIVE evidence: the construct is accepted AND produces the
#               right answer. stdout is the runtime observable.
#   reject      a conformance row of class `reject`. NEGATIVE evidence: the
#               compiler must refuse it with the declared diagnostic. FOR AN
#               INFERENCE FEATURE THE REJECTION IS THE PRODUCT — a region
#               inferencer that accepts everything is a no-op, and a no-op
#               disguises itself perfectly as green fixtures. That is exactly
#               what 07_traits_basic.pd did for a year.
#   skip        a conformance row of class `skip`, proven so by the compiler.
#   observable  a Rust integration test asserting a runtime property stdout
#               cannot show — wall-clock overlap, process exit status, the
#               structure of the generated C, a linked symbol.
#   gate        a make target that must exit 0.
#   decision    an owner decision that must be recorded before the row can be
#               written at all. A `blocked` row is not work; it is a question.
#
# status: satisfied | owed | blocked
#   satisfied   the evidence exists and is green — as of whenever the cited
#               evidence was last RUN, which is what `make m2-exit` inventory
#               four reports and what step 3 of the specification in
#               MILESTONES.md would make mechanical. This line used to read
#               "green TODAY (2026-08-22, 7484bac)": a date-and-sha assertion
#               in a manifest is a claim that expires, and that one had, three
#               commits and one day later. Do not put another one here — the
#               freshness of a row's evidence is a property of the last run,
#               not of a string in this file.
#   owed        the evidence does not exist or does not pass.
#   blocked     cannot be specified until the named decision is made.
#
# disposition: thesis | 1.0 | post-1.0
#   thesis      `make thesis-exit` reads this row directly. These rows ARE the
#               definition of 1.0.
#   1.0         in 1.0: the witnesses exercise it, or the thesis rows rest on it.
#   post-1.0    enumerated and explicitly deferred, owner `P1`. Recorded rather
#               than dropped, so the omission cannot become the next generation's
#               fiction.
#
# fingerprint: for a `reject` row, the diagnostic the refusal MUST carry; `-` otherwise.
#   A rejection is not evidence of a language property unless it is the INTENDED
#   rejection. "Refused because the prohibition is enforced" and "refused for
#   incidental unsupported syntax" are the same verdict to a runner that only sees
#   REJECTED — and a sibling branch can turn a reject fixture green with no compiler
#   change at all. So a thesis reject row NAMES the fingerprint, and the corpus row for
#   that fixture must declare it; scripts/conformance.sh has already matched the
#   declared fingerprint against the actual diagnostic.
#
# A milestone's exit is: no row owned by it is `owed` or `blocked`, and the
# reconciliation above holds. 1.0's exit is `make thesis-exit`.
# See docs/contributing/MILESTONES.md.
#
# N14-17 WAS ADDED 2026-08-23, and an added row is a contract transition like any
# other. Its evidence is `make stdlib-gate` and NOT a fixture: the row says EVERY
# normative builtin is callable, and a single driver spans a few of them. The gate
# holds the whole-registry invariant — Phase 3 requires the manifest's name set to
# equal the registry's, that no row is UNUSABLE, and that `reconcile` finds no
# `Support::Unsupported` entry at all. Evidence has to be as wide as the claim; the
# first version of this row cited `tests/stdlib/stdlib_builtins_file.pd`, which
# exercises the file family while the string and char/argument families are covered
# by other drivers entirely. MILESTONES.md's M2 item 6 DECLARED that `file_flush` and `file_seek` get
# re-based and that the four `*_ex` wrappers go, and no row of this manifest said
# so — which meant the M2 filter could have reached zero-owed with both builtins
# still uncallable and four dead wrappers still emitted into every program. That
# is the unowned-requirement hole this inventory exists to close, reproduced
# inside it. The work is done and the row records the obligation rather than the
# achievement: it goes red the moment any normative builtin is registered and
# refused again. `N14-01` is about the NAME SET and could not have caught it;
# `N14-03` is about SIGNATURES and belongs to M3.
#
# N3-13 MOVED M2 -> M5 ON 2026-08-26, and moving a row between milestones is a
# contract transition that this file's own doctrine says is reviewed as one. The
# review is the branch's, and the ground is a measurement rather than an
# estimate. "Macros are hygienic by default" was owned by M2 because the macro
# ROW NEXT TO IT was; nothing had ever run the hygiene probe. Run, on the
# unmodified compiler:
#   `macro m!() { secret }` with `let secret = 42;` at the CALL site printed 42
#   `macro m!() { n }` invoked from two functions with different local `n`
#     printed 1 and then 2 -- one macro body, two meanings, chosen by the caller
# Those are the two definitive capture signatures, and there is no hygiene
# machinery for them to have defeated: expansion renders the template back to
# SOURCE TEXT and re-lexes it (`src/macros/expander.rs`, `tokens_to_string`),
# which is textual substitution by construction and cannot be made hygienic by
# writing a fixture. The "hygiene by refusal" reading was tested and does not
# hold: the introduce-a-binding route is not defended by the shadowing rule, it
# is merely UNWRITABLE, because `let` in a macro body is refused for an
# unrelated reason (N3-12's token table). A defence that works only while a
# neighbouring bug survives is not a defence.
#
# So the row is an IMPLEMENTATION row wearing an evidence row's clothes, and its
# work sits with the macro system's other remaining work, which is already M5's:
# expansion to a fixed point, the `println!`/`assert!`/`dbg!` builtins, and the
# `tests/advanced_features_test.rs::test_macros` debt row. It stays `owed` and it
# stays enumerated -- the point of this file is that nothing is dropped
# silently, and a row that changes milestone has not been paid.
#
# id  milestone  source  requirement  evidence-kind  evidence  status  disposition

# --- N1 Overview and design commitments ------------------------------------
N1-01	M9	N1	Ahead-of-time compilation with no garbage collector	observable	tests/n1_no_gc.rs::no_collector_symbol_is_linked	owed	1.0	-
N1-02	M9	N1	No runtime type information	observable	tests/n1_no_rtti.rs::generated_c_carries_no_type_tags	owed	1.0	-
N1-03	M9	N1	Nothing in Part I depends on the target being C	gate	make selfhost-corpus	owed	1.0	-

# --- N2 Lexical structure ---------------------------------------------------
N2-01	-	N2	Identifiers	fixture	tests/01_lexical_literals.pd	satisfied	1.0	-
N2-02	-	N2	Integer literals	fixture	tests/01_lexical_literals.pd	satisfied	1.0	-
N2-03	M2	N2	Float literals	fixture	tests/02_types_floats.pd	satisfied	1.0	-
N2-04	M2	N2	Char literals	fixture	tests/02_types_chars.pd	satisfied	1.0	-
N2-05	-	N2	String literals	fixture	tests/01_lexical_literals.pd	satisfied	1.0	-
N2-06	-	N2	Boolean literals	fixture	tests/01_lexical_literals.pd	satisfied	1.0	-
N2-07	-	N2	Line comments	fixture	tests/01_lexical_comments.pd	satisfied	1.0	-
N2-08	M2	N2	Block comments NEST	fixture	tests/01_lexical_comments.pd	satisfied	1.0	-
N2-09	M2	N2	Escape sequences in char and string literals	fixture	tests/01_lexical_escapes.pd	satisfied	1.0	-
N2-10	M2	N2	Attributes lex: #[name], #[name(args)], #![name(args)]. THREE SHAPES, ONE EXECUTABLE WITNESS. The kind was `fixture` and that was UNSATISFIABLE, not unmet: `fixture` demands a conformance row of class `run` with a transcript, and N2-11 makes every attribute a compile error, so no program containing one can run. It is now `observable`, over the one test that drives ALL THREE shapes plus the two argument forms and asserts each is refused BY NAME -- reaching a diagnostic that says `frobnicate` is what proves the parser consumed `#`/`#!`, `[`, the name, any argument list and `]` before looking it up. A per-shape conformance fixture cannot carry that claim: one reject row names one file, and the corpus rows tests/reject/unknown_attribute.pd (bare), attribute_with_args.pd (paren-balanced, so `(a, b(c))` is ONE attribute) and attribute_inner.pd (`#!` as ONE token) each witness a single shape. The kind returns to `fixture` the day an attribute is implemented and a program carrying one can run	observable	tests/m2_lexical.rs::every_attribute_shape_is_refused_by_name	satisfied	1.0	-
N2-11	M2	N2	An unknown attribute is a compile error, not a silently ignored annotation	reject	tests/reject/unknown_attribute.pd	satisfied	1.0	unknown attribute `frobnicate`

# --- N3 Program structure and items -----------------------------------------
N3-01	-	N3	Functions with typed parameters and return types	fixture	tests/03_functions_basic.pd	satisfied	1.0	-
N3-02	M2	N3	A function body's trailing expression is its value, in every tail position	fixture	tests/stdlib/stdlib_tail_return.pd	satisfied	1.0	-
N3-03	M2	N3	A value-returning function with no value on some path is a compile error	reject	tests/reject/missing_return.pd	satisfied	1.0	-
N3-04	-	N3	Structs	fixture	tests/02_types_structs.pd	satisfied	1.0	-
N3-05	M2	N3	Enums with unit, tuple and struct variants	fixture	tests/02_types_enums.pd	satisfied	1.0	-
N3-06	M3	N3	Traits as items	fixture	tests/07_traits_basic.pd	owed	1.0	-
N3-07	-	N3	Impl blocks	fixture	examples/tutorial/04_structs.pd	satisfied	1.0	-
N3-08	M3	N3	Type aliases emit a real type	fixture	tests/03_type_alias.pd	owed	1.0	-
N3-09	M2	N3	Top-level const items	fixture	tests/03_const_items.pd	satisfied	1.0	-
N3-10	M2	N3	Top-level static items	fixture	tests/03_static_items.pd	satisfied	1.0	-
N3-11	M4	N3	Module items	fixture	tests/12_modules_imports.pd	owed	1.0	-
N3-12	M2	N3	One macro system, pattern-based	fixture	tests/03_macros.pd	satisfied	1.0	-
N3-13	M5	N3	Macros are hygienic by default	fixture	tests/03_macro_hygiene.pd	owed	1.0	-
N3-14	M2	N3	There is no macro_rules!/proc-macro split — macro_rules! is refused	reject	tests/reject/macro_rules.pd	satisfied	1.0	-
N3-15	M2	N3	Item order does not decide whether a program compiles, OVER THE BY-VALUE CONTAINMENT GRAPH. The scope is in the sentence on purpose: an unqualified "item order does not matter" is a claim about every axis on which one item can depend on another, and this row buys exactly one of them. A compilation unit is a SEQUENCE of items and the specification gives that sequence no meaning beyond containing them, so `struct S { e: E }` above `enum E { A, B }` is the same program as the reverse. It was not: code generation emitted type definitions in source order, C gives a field a size only from a complete type, and the first spelling died in gcc with `field has incomplete type 'struct E'` while the second ran. THE EVIDENCE IS A PERMUTATION, not an instance -- three declarations have six orders, exactly one of which source-order emission got right, so a fixture pinned to any single order would have stayed green for five wrong reasons. Order is now derived from the by-value containment graph the recursive-layout analysis already cut for N4-22, which is why a cut enum payload slot (a pointer, needing only the tag) constrains nothing. EXPLICITLY EXCLUDED, in the way N4-23 excludes its own case rather than leaving it to be discovered: TYPE ALIASES. Struct fields and enum payloads are converted by `ast_type_to_checker_type`, which resolves aliases out of `self.type_aliases`, and that map is filled INSIDE the same walk in source order -- so `type Small = i64;` above `struct S { x: Small }` checks and the reverse is refused with `Type mismatch: expected Small, found Int`. MEASURED on three revisions: `fb12f6f` (main) refuses BOTH orders, `5fbd1fe` and this revision accept the first and refuse the second, so this is a half-fix rather than a regression -- one order went from broken to working and none went the other way. It has no corpus witness either: a repo-wide scan finds no free top-level `type X = ...;` in any `.pd`. It is named here because a row that promises more than the code delivers is the defect this manifest exists to prevent, and closing the alias axis means pre-filling the alias map before the walk, which is not this row's work	observable	tests/m2_recursive_data_types.rs::every_declaration_order_of_one_program_gives_the_same_answer	satisfied	1.0	-

# --- N4 Types ----------------------------------------------------------------
N4-01	-	N4	i32 i64 u32 u64	fixture	tests/02_types_primitives.pd	satisfied	1.0	-
N4-02	M2	N4	f32 f64	fixture	tests/02_types_floats.pd	satisfied	1.0	-
N4-03	-	N4	bool	fixture	tests/02_types_primitives.pd	satisfied	1.0	-
N4-04	M2	N4	char -- a char literal LEXES and carries the right scalar (N2-04) and its TYPE is `char`, DISTINCT from i64 with no implicit conversion in either direction: print_int('a') and c == 97 are both type errors, and `as` is the only crossing. The named fixture witnesses the positive half (scalars, both cast directions, the round trip, the builtins without a cast); THE REFUSALS ARE PINNED SEPARATELY, since a run fixture cannot witness one -- tests/reject/char_is_not_an_int.pd and int_is_not_a_char.pd for the two directions of the split, char_arithmetic_is_not_an_int.pd for the operator branch, and four cast fixtures, one per forbidden direction (char<->bool, char<->float). It could not land alone and did not: N14-04 moved with it, or the literal would have had nowhere to go. `n as char` is CHECKED -- refused at compile time when written down (tests/reject/char_from_non_scalar.pd) and trapped when computed (tests/n4_char_traps.rs). One C carrier, long long, because a C char holds 8 bits and U+D55C needs 21	fixture	tests/02_types_chars.pd	satisfied	1.0	-
N4-05	-	N4	String	fixture	tests/02_types_primitives.pd	satisfied	1.0	-
N4-06	-	N4	unit ()	fixture	tests/03_functions_basic.pd	satisfied	1.0	-
N4-07	-	N4	int is an alias for i64	fixture	tests/02_types_primitives.pd	satisfied	1.0	-
N4-08	M7	N4	str and usize are named normatively — add them or rewrite the sites	decision	D3	blocked	1.0	-
N4-09	-	N4	Arrays [T; N] - WITNESSED FOR CONCRETE ELEMENT TYPES ONLY ([i64; 5], an inferred [i64; 3], [String; 3]). PBS-1 excludes generics, so no witness can exercise a parameterised T; the row is an instance claim, not a claim about every T	fixture	tests/02_types_arrays.pd	satisfied	1.0	-
N4-10	M2	N4	Nested arrays [[T; M]; N] as locals, parameters, struct fields and three deep; dimensions emitted after the identifier, outermost first -- all of that is what the named fixture runs. TWO REFUSALS BY NAME REMAIN, and they are pinned elsewhere because a positive fixture cannot witness a rejection: `for` over a nested array, where each step would bind a whole row (tests/reject/for_over_nested_array.pd), and an unresolved inner length in any declarator position (tests/reject/nested_array_param_inner_length.pd and its struct-field twin). The declarator shape itself is pinned by test_nested_array_parameter_declarator_and_inner_length_refusal in src/codegen	fixture	tests/02_types_nested_arrays.pd	satisfied	1.0	-
N4-11	M3	N4	Slices [T]	fixture	tests/02_types_slices.pd	owed	1.0	-
N4-12	M2	N4	Tuples (A, B) with construction and .0 access	fixture	tests/02_types_tuples.pd	satisfied	1.0	-
N4-13	M7	N4	References ref T / ref mut T are a distinct type to the checker	fixture	tests/05_references.pd	owed	1.0	-
N4-14	M3	N4	Function types fn(A) -> B	fixture	tests/02_types_fn.pd	owed	1.0	-
N4-15	M3	N4	Named generic types Name<A, B>, monomorphised	fixture	tests/08_generics_basic.pd	owed	1.0	-
N4-16	M3	N4	Option<T> and Result<T, E> exist as generic types with working methods	fixture	tests/08_option_result.pd	owed	1.0	-
N4-17	M8	N4	Option and Result are in the PRELUDE — in scope with no import	fixture	tests/08_prelude_in_scope.pd	owed	1.0	-
N4-18	M3	N4	? propagates a Result error to the caller	fixture	tests/09_question_mark.pd	owed	1.0	-
N4-19	M3	N4	? converts error types where a conversion exists	fixture	tests/09_question_mark_convert.pd	owed	1.0	-
N4-20	-	N4	Local type inference needs no annotation on let	fixture	examples/tutorial/01_variables.pd	satisfied	1.0	-
N4-21	M3	N4	Const generics are evaluated at compile time and monomorphised	fixture	tests/08_const_generics.pd	owed	1.0	-
N4-22	M2	N4	An `enum` whose payload names its own enum is a type -- it is laid out, built, taken apart by `match` and read back. N3 makes enums the sum type and N8's termination rule speaks of structural recursion on an inductive type, so an enum that cannot name itself leaves both without a subject. The recursive payload slot becomes a pointer; that is the compiler's ONE indirection and the row claims nothing wider. An INSTANCE claim, as every `satisfied` row here is: the witness is concrete and non-generic, because generic definitions are skipped by the analysis and `type_to_c` still erases their parameters	observable	tests/m2_recursive_data_types.rs::a_recursive_enum_compiles_links_and_runs	satisfied	1.0	-
N4-23	M2	N4	A by-value containment cycle with no such slot on it is refused BEFORE any C exists, and the refusal does not overstate its own reach. EXCLUDED ON PURPOSE, stated here so the exclusion outlives the limitation that currently hides it: a cycle broken only by a ZERO-LENGTH array (`struct Z { xs: [Z; 0] }`) is refused too, even though it stores no `Z` and therefore HAS a finite size. Two measured reasons -- `struct Z xs[0];` is an array of an incomplete element type, which cc refuses outright, and `[T; 0]` is uninhabited here because the empty array literal is refused. The wording is deliberately not `only an enum can stop a cycle`: cardinality can, and a requirement phrased on that theorem would certify more than the compiler delivers	reject	tests/reject/zero_length_array_self_reference.pd	satisfied	1.0	the refusal is a deliberate exclusion (requirement N4-23)

# --- N5 Statements and expressions -------------------------------------------
N5-01	-	N5	let, assignment, return, break, continue, expression statements	fixture	examples/tutorial/05_control_flow.pd	satisfied	1.0	-
N5-02	M5	N5	unsafe { } as a statement	fixture	tests/11_unsafe_blocks.pd	owed	1.0	-
N5-03	M2	N5	if is an expression: let x = if c { 1 } else { 2 };	fixture	tests/04_if_expression.pd	satisfied	1.0	-
N5-04	M2	N5	match is an expression	fixture	tests/06_match_expression.pd	satisfied	1.0	-
N5-05	M2	N5	Blocks are expressions	fixture	tests/04_block_expression.pd	satisfied	1.0	-
N5-06	M2	N5	else if	fixture	tests/04_else_if.pd	satisfied	1.0	-
N5-07	M2	N5	loop, exited by break, which may carry a value	fixture	tests/04_loop_break_value.pd	satisfied	1.0	-
N5-08	M3	N5	Closures with inferred capture, and a move form	fixture	tests/05_closures.pd	owed	1.0	-
N5-09	M3	N5	try { } scopes error handling	fixture	tests/09_try_block.pd	owed	1.0	-
N5-10	-	N5	Arithmetic and comparison operators	fixture	tests/02_types_primitives.pd	satisfied	1.0	-
N5-11	-	N5	Logical operators	fixture	tests/04_control_flow_if.pd	satisfied	1.0	-
N5-12	M2	N5	Bitwise operators & | ^ ~ << >>	fixture	tests/02_bitwise.pd	satisfied	1.0	-
N5-13	M2	N5	Compound assignment += -= *= /= %=	fixture	tests/02_compound_assign.pd	satisfied	1.0	-
N5-14	M2	N5	Ranges .. and ..= outside a for header	fixture	tests/04_ranges.pd	satisfied	1.0	-
N5-15	M2	N5	as casts	fixture	tests/02_as_casts.pd	satisfied	1.0	-
N5-16	M2	N5	Unary minus binds tighter than multiplication: a * -b	fixture	tests/02_unary_precedence.pd	satisfied	1.0	-
N5-17	M2	N5	Method call syntax x.f()	fixture	tests/04_method_calls.pd	satisfied	1.0	-

# --- N6 Patterns --------------------------------------------------------------
N6-01	-	N6	Wildcard and identifier patterns	fixture	tests/06_pattern_matching.pd	satisfied	1.0	-
N6-02	M2	N6	Literal patterns (integer, string, bool)	fixture	tests/06_literal_patterns.pd	satisfied	1.0	-
N6-03	M2	N6	Range patterns	fixture	tests/06_range_patterns.pd	satisfied	1.0	-
N6-04	-	N6	Enum path patterns with tuple and struct sub-patterns	fixture	tests/06_pattern_matching.pd	satisfied	1.0	-
N6-05	M2	N6	Tuple patterns	fixture	tests/06_tuple_patterns.pd	satisfied	1.0	-
N6-06	M3	N6	Slice patterns	fixture	tests/06_slice_patterns.pd	owed	1.0	-
N6-07	M2	N6	Or-patterns A | B	fixture	tests/06_or_patterns.pd	satisfied	1.0	-
N6-08	M2	N6	Binding patterns name @ pattern	fixture	tests/06_at_patterns.pd	satisfied	1.0	-
N6-09	M2	N6	Guards: arm if cond	fixture	tests/06_guards.pd	satisfied	1.0	-
N6-10	M2	N6	A non-exhaustive match is a compile error for EVERY scrutinee type	reject	tests/reject/nonexhaustive_int_match.pd	satisfied	1.0	-
N6-11	M2	N6	A match that falls through no arm traps rather than continuing	observable	tests/n6_match.rs::unmatched_scrutinee_traps	satisfied	1.0	-

# --- N7 Effects and asynchrony (differentiator 1) ------------------------------
N7-01	M5	N7	There is no async keyword	reject	tests/reject/async_fn.pd	owed	thesis	there is no `async` keyword
N7-02	M5	N7	There is no await operator	reject	tests/10_async_await.pd	owed	thesis	there is no await operator
N7-03	M5	N7	Effects are inferred from a body with no annotation	observable	tests/n7_effects.rs::io_builtin_makes_its_caller_effectful	owed	1.0	-
N7-04	M5	N7	Effects propagate to callers transitively	fixture	tests/09_effects_propagate.pd	owed	thesis	-
N7-05	M5	N7	Propagation is a FIXED POINT over the call graph, not a source-order pass	observable	tests/n7_effects.rs::callee_defined_below_its_caller_still_propagates	owed	1.0	-
N7-06	M5	N7	An unresolved callee is NOT assumed pure	reject	tests/reject/unknown_callee_not_pure.pd	owed	1.0	-
N7-07	M5	N7	Methods in impl blocks are effect-analysed	observable	tests/n7_effects.rs::impl_methods_are_analysed	owed	1.0	-
N7-08	M5	N7	"This function is pure" is a checkable claim	reject	tests/reject/pure_function_calls_io.pd	owed	thesis	declared pure
N7-09	M5	N7	The effect vocabulary is io, memory, panic and unsafe on one footing	fixture	tests/09_effects_system.pd	owed	1.0	-
N7-10	M5	N7	Effect contexts scope policy over a block	fixture	tests/09_effect_contexts.pd	owed	1.0	-
N7-11	M5	N7	effect::sync { } forces sequential execution	observable	tests/n7_parallel.rs::effect_sync_serialises	owed	1.0	-
N7-12	M5	N7	-> async T pins an asynchronous boundary	fixture	tests/09_async_boundary.pd	owed	1.0	-
N7-13	P1	N7	Independent effectful operations are parallel BY DEFAULT	observable	tests/n7_parallel.rs::two_independent_waits_overlap_in_wall_clock	owed	post-1.0	-
N7-14	P1	N7	May the compiler emit an unnamed execution substrate?	decision	D2	blocked	post-1.0	-
N7-15	P1	N7	Sequencing, cancellation and error semantics of parallel-by-default	decision	D2	blocked	post-1.0	-
N7-16	P1	N7	Effect tracking has no runtime representation	observable	tests/n7_parallel.rs::no_effect_metadata_in_generated_c	owed	post-1.0	-
N7-17	P1	N7	Structured concurrency: no orphaned task survives its scope	observable	tests/n7_parallel.rs::scope_exit_joins_or_cancels_every_branch	owed	post-1.0	-

# --- N8 Totality (differentiator 2) --------------------------------------------
N8-01	M6	N8	#[total] on a function	fixture	tests/13_total_attribute.pd	owed	thesis	-
N8-02	M6	N8	#![total(strict)] on a crate	fixture	tests/13_total_strict.pd	owed	1.0	-
N8-03	M6	N8	#[decreases(expr)] measure checking	fixture	tests/13_decreases.pd	owed	1.0	-
N8-04	M6	N8	#[total(fuel = N)] bounded termination	fixture	tests/13_total_fuel.pd	owed	1.0	-
N8-05	M6	N8	#[partial] opts out explicitly	fixture	tests/13_partial.pd	owed	1.0	-
N8-06	M6	N8	Structural recursion on a MONOMORPHIC inductive type needs no measure	fixture	tests/13_structural_recursion.pd	owed	thesis	-
N8-07	M6	N8	Structural recursion on a GENERIC inductive type needs no measure	fixture	tests/13_structural_recursion_generic.pd	owed	1.0	-
N8-08	M6	N8	A #[total] function whose obligation cannot be discharged is a compile error	reject	tests/reject/total_unproven.pd	owed	thesis	cannot prove termination
N8-09	M6	N8	A measure that does not strictly decrease is a compile error	reject	tests/reject/decreases_not_decreasing.pd	owed	1.0	-
N8-10	M6	N8	A #[total] function calling a #[partial] one is a compile error	reject	tests/reject/total_calls_partial.pd	owed	1.0	-
N8-11	M6	N8	unsafe is not permitted in a #![total(strict)] crate	reject	tests/reject/total_strict_unsafe.pd	owed	1.0	-
N8-12	M6	N8	There is NO mode in which an unproven #[total] function is accepted	observable	tests/n8_totality.rs::no_flag_downgrades_an_unproven_obligation	owed	1.0	-

# --- N9 References and lifetimes (differentiator 3) ----------------------------
N9-01	M7	N9	ref T is a shared borrow	fixture	tests/05_ref_shared.pd	owed	thesis	-
N9-02	M7	N9	ref mut T is a mutable borrow	fixture	tests/05_ref_mut.pd	owed	1.0	-
N9-03	M7	N9	ref<'a> T names a region explicitly, and is ACCEPTED	fixture	tests/05_ref_named_region.pd	owed	thesis	-
N9-04	M7	N9	There are no 'a parameter lists on functions, structs or impls	reject	tests/reject/lifetime_param_list.pd	owed	thesis	lifetime parameter list
N9-05	M7	N9	Lifetimes are inferred where they are not written	fixture	tests/05_lifetime_inference.pd	owed	1.0	-
N9-06	M7	N9	Inference failure is a compile error NAMING the ambiguity, never a guess	reject	tests/reject/ambiguous_region.pd	owed	thesis	ambiguous region
N9-07	M7	N9	Use after free is rejected	reject	tests/reject/use_after_free.pd	owed	1.0	-
N9-08	M7	N9	Two aliasing ref mut are rejected	reject	tests/reject/aliasing_ref_mut.pd	owed	1.0	-
N9-09	M7	N9	Borrow checking costs nothing at run time	observable	tests/n9_regions.rs::generated_c_carries_no_region_data	owed	1.0	-

# --- N10 Traits and generics ----------------------------------------------------
N10-01	M3	N10	A generic function is monomorphised per concrete argument	fixture	tests/08_generics_basic.pd	owed	1.0	-
N10-02	M3	N10	A generic struct or enum is monomorphised	fixture	tests/08_generic_types.pd	owed	1.0	-
N10-03	M3	N10	Foo<T> is a TYPE argument, not a const generic argument	fixture	tests/08_generics_basic.pd	owed	1.0	-
N10-04	M3	N10	Type parameter bounds <T: Display>	fixture	tests/07_trait_bounds.pd	owed	1.0	-
N10-05	M3	N10	where clauses	fixture	tests/07_where_clauses.pd	owed	1.0	-
N10-06	M3	N10	Trait methods take a self receiver and it PARSES	fixture	tests/07_traits_basic.pd	owed	1.0	-
N10-07	M3	N10	Trait method defaults	fixture	tests/07_trait_defaults.pd	owed	1.0	-
N10-08	M3	N10	Associated types	fixture	tests/07_associated_types.pd	owed	1.0	-
N10-09	M3	N10	Static dispatch through bounds — abstraction costs nothing at run time	observable	tests/n10_traits.rs::no_vtable_in_generated_c_for_a_bounded_call	owed	1.0	-
N10-10	M3	N10	Associated constants	fixture	tests/07_associated_consts.pd	owed	1.0	-
N10-11	M3	N7+N10	Function types and signatures reserve a LATENT EFFECT VARIABLE, so M5 does not redesign them	observable	tests/n10_effect_variable.rs::fn_type_carries_an_effect_slot	owed	1.0	-

# --- N11 Modules -----------------------------------------------------------------
N11-01	M4	N11	File-based modules with nested paths	fixture	tests/12_modules_imports.pd	owed	1.0	-
N11-02	M4	N11	Public/private visibility is ENFORCED	reject	tests/reject/private_item_import.pd	owed	1.0	-
N11-03	M4	N11	import std::math;	fixture	tests/12_import_plain.pd	owed	1.0	-
N11-04	M4	N11	import std::io::{read, write};	fixture	tests/12_import_selective.pd	owed	1.0	-
N11-05	M4	N11	import std::collections as col;	fixture	tests/12_import_alias.pd	owed	1.0	-
N11-06	M4	N11	import std::prelude::*;	fixture	tests/12_import_glob.pd	owed	1.0	-
N11-07	M4	N11	A cross-file call resolves	fixture	tests/projects/hello_pdm/tests/test_math.pd	owed	1.0	-

# --- N12 Memory model --------------------------------------------------------------
N12-01	-	N12	Each value has one owner; moves are the default	fixture	tests/05_ownership_basic.pd	satisfied	1.0	-
N12-02	-	N12	Borrows are checked at compile time	fixture	tests/05_borrowing_references.pd	satisfied	1.0	-
N12-03	M7	N12	Values with a destructor are dropped at end of scope	observable	tests/n12_drop.rs::drop_glue_runs_at_scope_exit	owed	1.0	-
N12-04	M7	N12	String is an owned heap value with MOVE semantics	reject	tests/reject/use_of_moved_string.pd	owed	1.0	-
N12-05	M7	N12	A moved-from binding cannot be read	reject	tests/reject/use_after_move.pd	owed	1.0	-
N12-06	M7	N12	&mut / ref mut may be taken only of a binding declared mut — for EVERY referent type	reject	tests/reject/ref_mut_of_immutable_struct.pd	owed	1.0	-
N12-07	M5	N12	unsafe { } is auditable and isolated	fixture	tests/11_unsafe_blocks.pd	owed	1.0	-
N12-08	M7	N12	Do [T; N] parameters copy or alias?	decision	D4	blocked	1.0	-
N12-09	M7	N12	Whatever D4 decides, the rule is enforced in the TYPE SYSTEM	reject	tests/reject/array_param_rule.pd	blocked	1.0	-

# --- N13 Execution model -------------------------------------------------------------
N13-01	-	N13	Execution begins at fn main	fixture	tests/03_functions_basic.pd	satisfied	1.0	-
N13-02	-	N13	A compilation unit without main is a library	skip	tests/projects/hello_pdm/src/math.pd	satisfied	1.0	-
N13-03	M2	N13	Arguments are evaluated left to right -- and the named fixture witnesses the OBSERVABLE ORDER only. The GUARANTEE is structural and is pinned in src/codegen by test_argument_reads_are_sequenced_left_to_right, because C leaves the order unspecified and this host's gcc happened to choose left-to-right: the fixture would have passed before the rule existed. What the rule says is that in a call with at least one effectful argument, every argument READ happens at that argument's own position. Short-circuit composition is pinned by test_sequenced_arguments_stay_inside_a_short_circuit_guard and the place-argument shapes by test_place_arguments_are_sequenced_as_pointers	fixture	tests/03_arg_evaluation_order.pd	satisfied	1.0	-

# --- N14 Builtins and the standard library ---------------------------------------------
N14-01	M2	N14	The builtin set is exactly the 34 normative names	observable	src/builtins.rs::test_registry_is_exactly_the_normative_builtin_set	satisfied	1.0	-
N14-02	M2	N14	A program cannot define or shadow a builtin name -- at all ELEVEN binding positions, counted from the distinct `what` slots in src/typeck: six definitions (fn, struct, enum, type alias, top-level const, top-level static) and five binders (parameter, local, loop variable, pattern binding, @ binding). Two different reasons, named by the diagnostic: a definition is C nothing can reach, a type collides in no C namespace but breaks the one-namespace rule. All eleven are pinned by a_builtin_name_is_refused_at_every_binding_position (src/typeck); the named fixture is the definition position, with tests/reject/shadow_builtin_parameter.pd and shadow_builtin_type.pd for the binder and the second reason, and the over-refusal control (near-miss names still compile) in tests/03_functions_basic.pd	reject	tests/reject/shadow_builtin.pd	satisfied	1.0	-
N14-03	M3	N14	Builtin signatures match N14 — filesystem builtins return Result	fixture	tests/stdlib/stdlib_builtins_file.pd	owed	1.0	-
N14-04	M2	N14	string_char_at returns char -- and the named fixture DISCRIMINATES rather than merely passing: it binds the result to an annotated `char`, which an i64 return fails with 'expected Char, found Int'. Every other assertion in it would have compiled either way, because `i64 as i64` is a legal cast. The -1 out-of-range sentinel died with the type (-1 is not a char, and no program in the corpus read it); an index with no character behind it TRAPS instead, which no fixture can observe because a trap ends the process -- that half is pinned by tests/n4_char_traps.rs, at index == len and at -1, with the last valid index as the over-refusal control	fixture	tests/stdlib/stdlib_builtins_string.pd	satisfied	1.0	-
N14-17	M2	N14	Every normative builtin is CALLABLE — none is registered-and-refused	gate	make stdlib-gate	satisfied	1.0	-
N14-05	M5	N14	The effect classification of every builtin is ENFORCED	reject	tests/reject/pure_calls_file_open.pd	owed	1.0	-
N14-06	M8	N14	Every file under stdlib/ is accepted by the compiler	gate	make stdlib-gate	owed	1.0	-
N14-07	M8	N14	stdlib/ is on the default search path with no configuration	observable	tests/n14_stdlib.rs::import_std_works_with_no_env	owed	1.0	-
N14-08	M8	N14	stdlib/ is installed by every shipping package	observable	tests/n14_stdlib.rs::both_formulae_install_the_stdlib	owed	1.0	-
N14-09	M8	N14	Collections: Vec<T>	fixture	tests/stdlib/std_vec.pd	owed	1.0	-
N14-10	M8	N14	Collections: HashMap<K, V>	fixture	tests/stdlib/std_hashmap.pd	owed	1.0	-
N14-11	M8	N14	String methods above the builtins	fixture	tests/stdlib/std_string.pd	owed	1.0	-
N14-12	M8	N14	An iterator protocol, expressed as a trait	fixture	tests/stdlib/std_iterator.pd	owed	1.0	-
N14-13	M8	N14	Math module	fixture	tests/stdlib/std_math.pd	owed	1.0	-
N14-14	M8	N14	Buffered I/O	fixture	tests/stdlib/std_io_buffered.pd	owed	1.0	-
N14-15	M8	N14	Networked I/O	fixture	tests/stdlib/std_net.pd	owed	1.0	-
N14-16	M8	N14	Process and environment access	fixture	tests/stdlib/std_process.pd	owed	1.0	-

# --- The thesis gate. 1.0 IS these rows: the differentiated dialect, proven on a compiler
# --- that cannot pass them vacuously. `make thesis-exit`. ---------------------------------
TH-01	M9	N7	bootstrap/pdc.pd contains no `async` and no `await` token	gate	make thesis-exit	owed	thesis	-
TH-02	M9	N9	bootstrap/pdc.pd contains no lifetime PARAMETER LIST (ref<'a> stays legal)	gate	make thesis-exit	owed	thesis	-
TH-03	M9	N9	bootstrap/pdc.pd declares at least one ref / ref mut parameter	gate	make thesis-exit	owed	thesis	-
TH-04	M9	N8	bootstrap/pdc.pd carries at least one #[total] the compiler DISCHARGES	gate	make thesis-exit	owed	thesis	-
TH-05	M9	N7	A file-IO function's inferred effect propagates to its caller and passes the gate	gate	make thesis-exit	owed	thesis	-
TH-06	M9	N1	The second witness meets the same three conditions, so one program's shape is not the language	gate	make thesis-exit	owed	thesis	-
WT-01	M2	N1	Witness 2 exists: a JSON parser written with no workarounds, in the corpus	fixture	tests/witness/json_parser.pd	owed	1.0	-
WT-02	M9	N1	Witness 2 is rewritten in the differentiated dialect and still runs	fixture	tests/witness/json_parser.pd	owed	thesis	-

# --- C FFI. The one feature-list-only item kept in 1.0: the backend already emits C, and a
# --- systems language whose IO cannot leave the builtin set is not 1.0. -------------------
FFI-01	M8	V1-8	A Palladium program can call a C function declared to it	fixture	tests/ffi/call_c.pd	owed	1.0	-
FFI-02	M8	V1-8	A Palladium function can be exported to C and called from it	observable	tests/ffi.rs::exported_symbol_is_callable_from_c	owed	1.0	-
FFI-03	M8	V1-8	The FFI boundary is effect-classified, not a hole in N7	reject	tests/reject/ffi_escapes_effects.pd	owed	1.0	-

# --- Self-hosting: the project's own claim about pdc -------------------------------------
SH-01	-	A12	bootstrap/pdc.pd compiles itself to a byte-identical fixed point over PBS-1 - MEASURED UNDER TWO EXCLUSIONS THIS ROW NOW STATES, NOT ONE: the unit imports NOTHING (grep -c '^import' bootstrap/pdc.pd = 0) and uses NO GENERICS (grep -cE 'fn [a-zA-Z_]+<' bootstrap/pdc.pd = 0; docs/specification/bootstrap-subset.md:76 excludes them from PBS-1), and pdc.pd:8 states the exclusion as a virtue - 'This file is written in exactly the subset it implements'. So THIS ROW IS EVIDENCE THAT PBS-1 AVOIDS BOTH UNORDERED EMISSION SOURCES, NOT THAT THE COMPILER IS DETERMINISTIC. Byte identity for a unit that imports OR is generic is SH-05	gate	make selfhost	satisfied	thesis	-
SH-05	M9	src/codegen/mod.rs:81	Module emission AND generic-instantiation emission are ORDER-DETERMINISTIC, so the fixed point survives the rewrite. TWO INDEPENDENT SOURCES, SEPARATED BY MEASUREMENT AND NOT BY INFERENCE. (a) MODULES: imported modules live in a HashMap (src/codegen/mod.rs:81) emitted by iterating .values() (src/codegen/mod.rs:1140, :1193, :1278); measured, 8 compiles of one unchanged two-module program produced 2 distinct SHA-1s, 4 each - a pure ordering swap of two function bodies. (b) GENERICS: instantiations live in HashMaps (src/typeck/mod.rs:321, :329) emitted by iterating .keys() (src/typeck/mod.rs:2931, :2948) and consumed at src/codegen/mod.rs:1316, :1774; measured, six generics with ZERO imports produced 30 distinct outputs in 30 compiles - generics alone break byte-stability with no module in sight, and six modules with no generics was byte-identical. NEITHER HALF DISCHARGES THE OTHER: ordering the module iteration leaves (b) standing, and ordering the instantiation iteration leaves (a) standing. NEITHER IS A DEFECT REPORT FILED AGAINST SOMEONE ELSE: repairing a mechanism retires no part of this row, because the requirement is that byte identity survive the REWRITE of pdc.pd into the differentiated dialect, which uses imports and generics both. This row is satisfied only by a rewritten unit that exercises both and still reaches the fixed point	gate	make selfhost-determinism	owed	thesis	-
SH-02	M9	A12	bootstrap/pdc.pd compiles EVERY fixture in the conformance corpus	gate	make selfhost-corpus	owed	thesis	-
SH-03	M9	A12	Its output matches the Rust compiler's on every fixture, acceptances and refusals alike	gate	make selfhost-corpus	owed	thesis	-
SH-04	M9	A12	PBS-1 is the whole 1.0 language, not a subset	gate	make selfhost-corpus	owed	thesis	-

# --- Gate integrity: the gates must be able to fail ---------------------------------------
GI-01	-	A11	The conformance corpus is a closed inventory	gate	make conformance	satisfied	1.0	-
GI-02	-	A11	The conformance runner still goes RED when it should	gate	make test-conformance-runner	satisfied	1.0	-
GI-03	-	A8	Every evidence producer the stdlib gate reads is fault-injected	gate	make test-gate-probe	satisfied	1.0	-
GI-04	-	-	Every documentation snippet compiles and every citation resolves	gate	make check-docs	satisfied	1.0	-
GI-05	-	-	Every test binary runs, integration tests included	gate	make test-honest	satisfied	1.0	-
GI-06	M2	-	make gates runs test-honest, so a non-ignored regression cannot coexist with a green gate	gate	make gates	satisfied	1.0	-
GI-07	-	-	Every declared failure names an owning milestone and none of them passes	gate	make test-xfail	satisfied	1.0	-
GI-08	M2	-	Every milestone exit reads BOTH debt inventories and this manifest. THERE ARE TWO MILESTONE EXITS -- `make m1-exit` and `make m2-exit` -- and both read all four inventories now, which is the whole of the claim. m1-exit did not, for a measured reason: this manifest owns no M1 row, so scripts/requirements.py abstains with exit 2, and the old `|| rc=1` aggregation would have reddened a legitimately green target for a reason that says nothing about M1. scripts/m1-exit.sh reads it anyway and tells that ONE exit-2 shape apart from the other two (unreadable manifest; evidence unresolved) by the sentence each prints, tolerates it with the sentence REPRINTED, fails closed on the others, and never masks an exit-1 measurement. THE EVIDENCE COLUMN NAMES THE RUNNER AND NOT `make m2-exit`: m2-exit is RED by design until M2 lands, so it can never witness a `satisfied` row, and it says nothing about m1-exit either way. make test-requirements-runner RUNS BOTH targets and asserts, per target, that each of the four inventories left evidence of its own invocation, plus the six controls over m1-exit's tolerance	gate	make test-requirements-runner	satisfied	1.0	-
GI-09	M2	-	The owner filter is self-tested: it detects a row planted for the milestone under test	gate	make test-requirements-runner	satisfied	1.0	-
GI-10	M9	-	1.0 exits on the whole manifest plus the gates plus test-honest	gate	make v1-exit	owed	1.0	-
GI-12	M2	-	A rejection is ATTRIBUTABLE: pdc emits a stable diagnostic CODE, every reject row pins a CODE not a phrase, and an incidental phrase cannot satisfy one. Adjudicated OUTSIDE the substring matcher it replaces	gate	make check-diagnostic-codes	owed	thesis	-
GI-11	M3-start	-	pdc EXPORTS a release-grade call graph and the gate DERIVES TH-03/04/05 from feasible ordered paths from declared roots, over the projection whose callees are DEFINED IN THE SUBMITTED UNIT (builtin callees are outside the exported projection, so a graph that models them answers these queries without them): scoped call-site identities (not bare names), source-order-independent fixed point, the provider returning ONE provenance-bearing graph SNAPSHOT per unit, the gate invoking it ONCE per unit and PROJECTING every property from a copy of what came back - a claim about the runner, which establishes neither that the provider assembled that container atomically nor that the graph was DERIVED from those bytes, and replaces a per-answer digest that proved only that the provider could hash its input, may-complete/divergence per edge, calls in conditions/initializers/return expressions, every indirect call site answered SEPARATELY AND KEYED BY POSITION - caller plus the 1-based index of the site within it, an alphabet with no delimiter in it, because a scalar answer cannot address one site of two and a key built from source text cannot round-trip - either as a resolution naming one or more targets or as unresolved - both discharge the obligation, and omitting the call site silently is a graph FAILURE, while an answer that claims resolution for a site and names NO target is a HARNESS FAILURE distinct from omission, because the gate cannot tell a resolution from a declination and refuses to score the row rather than read it as either - and fault injection proving each verdict changes when its edge or completion fact is removed; adjudicated by tests/callgraph-differential.tsv and tests/liveness-differential.tsv, NOT by a named test whose mere passing an empty body satisfies	gate	make thesis-exit	owed	thesis	-

# --- Owner decisions. A blocked row above cannot be written until these are answered. -----
D1-01	M9	F5	RESOLVED 2026-08-22: neither option. 1.0 IS the thesis gate; this manifest enumerates, it does not gate	gate	make thesis-exit	owed	thesis	-
D2-01	P1	N7	May the compiler emit an unnamed execution substrate, and with what semantics?	decision	D2	blocked	post-1.0	-
D3-01	M7	N4	str and usize: add the primitives, or rewrite the normative sites	decision	D3	blocked	1.0	-
D4-01	M7	N12.1	Array parameters: value semantics or reference semantics	decision	D4	blocked	1.0	-
