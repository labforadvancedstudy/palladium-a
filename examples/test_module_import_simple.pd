// Test module imports - for now using direct calls

fn pd_abs(x: i64) -> i64 {
    if x < 0 {
        return -x;
    }
    return x;
}

fn main() {
    let x = -42;
    let y = pd_abs(x);
    print_int(y);    // Should print 42
}